# Grafana datasources - auto-provisioned on startup.
# Prometheus is used for L1/L2/L3 metrics (payment-app /actuator/prometheus).

apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false